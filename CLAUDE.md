# CLAUDE.md

Follow `AGENTS.md` first.

This repository contains AuxiTalk plugin templates. Keep generated plugin code safe, minimal, testable, and compliant with JSON-RPC 2.0 over stdio.

Before finishing code changes, run the relevant tests for the template language.

Never write plugin logs to stdout. stdout is reserved for JSON-RPC messages.
